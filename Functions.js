
function greet(user){
    return `Hello ${user}!!`;
}

let user = 'Lily';
let str = greet(user); // Lili

console.log(str);

/* -------------------------------------------------------------------------- */

function greet2(a, b) {
    return `price = ${a * b} ლარი.`;
}

  
  console.log(greet2(5, 10));

